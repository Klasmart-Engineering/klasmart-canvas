import { fabric } from 'fabric';
import React from 'react';
export declare const textHandler: (text: string, fontFamily: string, updateFontFamily: React.Dispatch<React.SetStateAction<string>>) => fabric.Text;
