# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more information see: https://help.github.com/actions/language-and-framework-guides/publishing-nodejs-packages

name: Node.js Package

on:
  release:
    types: [created]

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-node@v2
  #       with:
  #         node-version: 16
  #         token: ${{ secrets.PAT_NPM_GITHUB_REPO }}
  #     - run: npm install npm@latest -g
  #     - run: npm i --legacy-peer-deps
  #       env:
  #         NPM_GITHUB_TOKEN: ${{secrets.PAT_NPM_GITHUB_REPO}}


  publish-gpr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - name: Install xmllint
        run: sudo apt-get install -y cairo cairo-devel cairomm-devel libjpeg-turbo-devel pango pango-devel pangomm pangomm-devel giflib-devel
      - run: npm i
      - run: cat package-lock.json
      - run: npm run build
      - run: npm publish
        env:
          NPM_GITHUB_TOKEN: ${{secrets.PAT_NPM_GITHUB_REPO}}
